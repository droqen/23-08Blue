use ambient_api::prelude::*;

// use ambient_api::core::player::components::{player, user_id};

#[main]
pub fn main() {
    // spawn_query((player(), user_id())).bind(|players|{
    //     for (player,(_,uid)) in players {
    //         // spawn boat.
    //     }
    // });
    // set boat movement by player cursor?
}